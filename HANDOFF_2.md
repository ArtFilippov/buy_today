# Handoff 2: общий конвейер завершён, дальше Docker и GitHub Actions

Обновлено: **2026-09-22**. Репозиторий: `/home/user/prak`. Общение — на русском.

**Точка продолжения:** локальные `run`, `inference` и `summary` реализованы; новый прогон SVD завершил все девять батчей. Следующий этап — прочитать `320_MLOps_task_2.pdf`, согласовать инфраструктурные интерфейсы и реализовать Docker, затем GitHub Actions / сценарий CD.

## 1. Последние решения пользователя

- **MiniBatchKMeans и новое расстояние пропускаем.** Работаем с временной кластеризацией, TimestampDistance и существующими ранжировщиками. Прежний план признаков сохранён в [README](README.md#minibatchkmeans-отложенный-план).
- **Один выбранный ранжировщик на прогон:** Random или SVD. `run` не сравнивает модели и не выбирает победителя. Inference получает явно указанный каталог модели.
- **Новый прогон с `batch_000`**, отдельный каталог результатов. Прежние эксперименты 000/001 не подхватываются как состояние координатора.
- **При ошибке должно быть выброшено исключение.** Новые команды передают исходное исключение наружу; автоматического восстановления нет. Частичные результаты могут остаться, индекс шага не продвигается. Повтор на незавершённом шаге также завершается исключением.
- Согласованный долгосрочный объём — **MVP + Docker + GitHub Actions**, без специализированных MLOps-платформ.

## 2. Состояние кода и проверок

- База этапа: **`7e42b64 add SVD ranking and baseline comparison`**. По запросу пользователя текущий этап сохраняется коммитом **`add pipeline orchestration, inference and summary`**. Перед продолжением проверить `git status` и `git log -1`.
- Полная проверка после исправлений: **`uv run --frozen pytest` — 777 passed за 498.24 с**. После этого менялась только документация. Тесты используют искусственные данные, Olist не требуется.
- Проверены реальный двухбатчевый цикл с notebook, обе модели по отдельности, повторяемость новых прогонов, сохранность историй, фиксация конфигурации, повреждение входов, подмена внутренне корректного снимка историй, незавершённые шаги и исходные исключения.
- `threadpoolctl` теперь объявлен прямой зависимостью; `uv.lock` обновлён. Установка: `uv sync --frozen`.

### Точки входа

| Задача | Реализация |
| --- | --- |
| Полный шаг и остаток потока | `src/prak/pipeline.py`: `PipelineConfig`, `run_next_batch`, `run_pipeline` |
| Настройки и завершённые шаги | Там же: `read_pipeline_config`, `read_completed_steps` |
| Рекомендации сохранённой модели | `src/prak/ranking/inference.py`: `recommend`, `export_recommendations` |
| Сводка одного прогона | `src/prak/summary.py`: `report_summary` |
| CLI | `src/prak/cli.py`: `run`, `inference`, `summary` плюс прежние команды |
| JSON результатов отчётов | `auto_eda/{eda,deda,notebook}.py`, `clustering/report.py` |
| Новые проверки | `tests/test_pipeline.py`, `tests/test_summary.py`, `tests/ranking/test_inference.py` |

Полные команды, схемы, параметры и метрики — в [README: общий запуск](README.md#общий-последовательный-запуск), [inference](README.md#получение-рекомендаций-inference), [summary](README.md#сводка-прогона-summary).

## 3. Готовый реальный прогон

Каталог: **`models/olist-stream/runs/svd/`**. Внутри — `config.json`, `state.json`, физический `reference.csv`, девять `steps/step_000` … `step_008` и `summary/`.

- Состояние прогона: **`next_batch_index=9`**, поток завершён.
- Эталон: **81 387 строк × 35 колонок** — точное объединение девяти поступивших батчей. Рабочий CSV содержит 82 775 строк; хвост 1 388 строк в поток не включён.
- Истории последнего шага: **4 000 пользователей, 400 000 событий**, split **280 000 / 60 000 / 60 000**, каталог **25 448 товаров**.
- Зафиксированы SVD `n_components=32`, `n_iter=7`, seed `42`, температура `86400` секунд, `2000 + 8×250` пользователей, split `70/15/15`, K `10`.
- Конечные validation **Recall@10 = 0.060832805, NDCG@10 = 0.103725339**; test **Recall@10 = 0.062748318, NDCG@10 = 0.106668710**.
- Силуэт шага 008 — **0.485387739**. Дрейф на шагах 001, 002, 005–008 информационный; все обновления прошли.
- На каждом переходе проверено точное сохранение прежних событий/якорей/источников, накопление каталога и происхождение батчей. Конечный снимок прошёл полную проверку `read_history_dataset`.
- Первые два шага нового прогона **побайтово совпали** с прежними историями, SVD-моделями и индивидуальными CSV оценок. Это проверка интеграции с прежним экспериментом; полный девятибатчевый прогон выполнен один раз.
- Inference проверен на шагах 001 и 008: `recommendations_step_001.csv`, `recommendations_step_008.csv` в корне прогона. Проверено корректное исчерпание потока при повторном `run --all`.
- Готовая сводка: **`models/olist-stream/runs/svd/summary/summary.html`**, рядом JSON/CSV. Все размеры и метрики шагов опубликованы в README.
- Временной проверочный скрипт: `/tmp/opencode/verify_prak_pipeline.py`; это вспомогательный локальный файл, не часть поставки.

### Другие сохранённые артефакты

- Источник — `dataset/`; подготовленный поток — `data/olist-stream/batches/`, манифест — `data/olist-stream/manifest.json`.
- `data/olist-stream/state.json` остаётся артефактом подготовки с индексом `0`. Общий запуск использует своё состояние внутри `--run-dir`.
- Прежние истории — `data/olist-stream/histories/step_{000,001}/`.
- Прежние модели/оценки — `models/olist-stream/ranking/{random,svd}/step_{000,001}/`.
- Прежний отчёт временной модели — `models/olist-stream/clustering/temporal/batch_000/`.
- `dataset/`, `data/` и `models/` игнорируются Git. Для нового эксперимента нужен новый `--run-dir`.

## 4. Контракты, которые важно сохранить

### Координатор и файлы

- Первый шаг: эталон/EDA → кластеризация/оценка → истории → один ранжировщик → validation/test. Далее DEDA выполняется до добавления, EDA — после.
- Модельный этап использует накопленный эталон, генератор — только новый батч и предыдущий снимок, ранжировщик — накопленный train и каталог.
- Конфигурация фиксируется в начале, путь/параметры при продолжении должны совпасть. Индекс продвигается только после всех этапов. Состояние одного файла заменяется атомарно; весь шаг не является транзакцией.
- Манифест шага закрепляет SHA-256 метаданных, включая манифест генератора. Внутренние файлы истории дополнительно проверяет сам генератор. Это предотвращает подхват другого корректного, но чужого снимка.
- Модели и оценки ранжирования используют один поток BLAS/OpenMP. Для t-SNE лимит передаётся окружению отдельного notebook-ядра, а не только процессу координатора.
- `reference.csv` содержит текущее накопление. Для повторного исполнения notebook старого шага потребуется соответствующий исторический CSV; готовый HTML уже содержит результаты своего шага.
- Ошибка после записи эталона не откатывает его. Повторный вызов блокируется контрольной суммой/существующим каталогом шага. Восстановление ручное либо новый прогон.
- `summary` читает завершённые шаги и JSON результатов, проверяет их идентичность; исходные CSV, модели и старые HTML для сборки не требуются. Частичный шаг игнорируется.

### Данные, генерация, модели

- Единица Olist — позиция заказа; ключ `(order_id, order_item_id)`, схема 35 колонок в `schema.py`. `working_dataset.csv` включает будущие строки и хвост и не заменяет эталон.
- Дрейф — информационный результат; нарушения данных/пересечение ключей вызывают исключения. EDA/DEDA исполняются в notebook, по три графика на отчёт. JSON экспортируется из тех же расчётов.
- Число кластеров принадлежит реализации алгоритма. Временной бейзлайн полностью переобучается на эталоне и пересчитывает все метки.
- Расстояние имеет `fit` и `pairwise(X, Y=None)`. TimestampDistance — абсолютная разница времени покупки в секундах.
- Силуэт и t-SNE используют одну квотную выборку до 1000 строк и одну матрицу расстояний. Квота — половина новых/прежних строк с добором.
- Ранжировщик связан с кластеризацией **только через моделируемые данные**. Генератор использует расстояние, а не метки кластеров. Якорь выбирается равномерно по строкам; покупки с возвращением по `exp(-d / temperature)`.
- Каждый батч добавляет новых модельных пользователей. Старые пользователи, события, split и якоря сохраняются точно. Синтетический `user_id` отличается от Olist `customer_unique_id`.
- Разбиение историй идёт по синтетическому `event_index`. Каталог включает все товары поступивших батчей, включая не выбранные в покупки.
- Вход ранжировщика — `RankingData(interactions, catalog)`, взаимодействия ровно `user_id/product_id` с повторами; выдача — `predict(user_id, k)`. Купленные товары разрешены, неизвестный пользователь и K вне каталога — ошибки.
- SVD обучается на счётчиках train-покупок, включает весь каталог, обнуляет факторы товаров без train-событий. Равные score разрешаются по `product_id`; размерность не уменьшается автоматически.
- Recall/NDCG используют уникальные отложенные товары и макроусреднение по пользователям; товары вне train остаются в ground truth. Оценка вызывает только `predict`, не обучает модель и не читает якоря.
- Историческое сравнение Random/SVD было на одинаковых снимках. Новый координатор использует одну модель на прогон; автоматический выбор лучшей пользователь отклонил.

## 5. Следующие действия: инфраструктура

1. Проверить Git и прочитать **`320_MLOps_task_2.pdf`**. Первый документ задания — `320_MLOps_task_1.pdf`; границы локального решения и команды уже описаны в README.
2. Определить Docker-интерфейс: образ Python/uv, файловые входы и каталоги результатов через volumes, запуск `prak run`, `inference`, `summary`, исполнение notebook внутри контейнера. Критерий завершения — проверенный контейнерный запуск на небольшом потоке с доступными снаружи результатами.
3. Согласовать GitHub Actions: проверки кода и тестов, сборка образа, источник тестовых данных, публикация образа/артефактов и конкретный сценарий CD. Эти решения пока не приняты.
4. После реализации записать реальные команды и результаты в README, обновить точку продолжения здесь.

Навыки по ситуации: `implement-spec` для согласованной реализации; `wizard` для действительно пользовательских действий с инфраструктурой/секретами; `writing-for-agents` для обновления handoff.

# Handoff 2: SVD завершён, переход к MiniBatchKMeans

Обновлено: **2026-09-22**. Репозиторий: `/home/user/prak`. Общение — на русском.

**Точка продолжения: определить оставшиеся решения для MiniBatchKMeans и нового расстояния, затем реализовать этот модельный этап.** SVD подключён к Python API и CLI, проверен и сравнен с RandomRanker на сохранённых историях шагов 000 и 001. Полная проверка — 654 теста; повторные SVD-эксперименты воспроизводимы. Вопросы следующего этапа — разделы 2.2 и 5.

Этот документ объединяет актуальные решения прежних handoff, удалённых по запросу пользователя. Он хранит приоритеты, принятые ограничения и открытые вопросы. Фактические команды, Python API, схемы файлов и результаты готовых этапов — в [README.md](README.md).

## 1. Текущее состояние

| Этап | Статус и точка входа |
| --- | --- |
| Подготовка Olist и хронологические батчи | Готово: `prepare`, [README](README.md#подготовка-данных-и-батчей) |
| EDA/DEDA и физический эталон | Готово: `eda`, `deda`, `init`, `update`, [README](README.md#eda-и-создание-эталона) |
| Временная кластеризация и расстояние | Готово: `cluster`, `evaluate`, [README](README.md#временной-бейзлайн-кластеризация-и-оценка) |
| Генерация и накопление модельных историй | Готово: `generate`, [README](README.md#модельные-пользовательские-истории) |
| RandomRanker, сохранение и независимые метрики | Готово: `rank`, `evaluate-ranking`, [README](README.md#ранжирование-и-независимая-оценка) |
| SVD и сравнение с Random | Готово: `rank --model svd`, [README](README.md#ранжирование-и-независимая-оценка) |
| MiniBatchKMeans, общий запуск, inference, summary, Docker, CI/CD | Дальнейшие этапы, см. раздел 2 |

### Проверки, Git и локальные артефакты

- База этапа SVD — `434d38f` (`add random ranking baseline and independent evaluation`). По запросу пользователя «Закоммить всё» код SVD, CLI, тесты и документация сохраняются коммитом `add SVD ranking and baseline comparison`. Перед продолжением проверить `git status` и `git log -1`.
- Последняя полная проверка после реализации SVD: **`uv run --frozen pytest` — 654 passed за 398 с**. Отдельно `uv run --frozen pytest tests/ranking -q` — **157 passed**, на этом этапе добавлено 52 проверки. После этого менялась только документация.
- В `data/olist-stream/` подготовлены **82 775 строк × 35 колонок, 20 категорий**; поток — `batch_000` на **41 387** строк и `batch_001` … `batch_008` по **5 000**. Хвост **1 388** строк остаётся только в рабочем CSV.
- На момент handoff `reference.csv` и отчётов нового потока эталона нет. Для его продолжения нужен `init batch_000`. Исторические EDA/DEDA до фильтрации редких категорий описаны в README; их размеры не относятся к новому потоку.
- Временной бейзлайн проверен на `batch_000`: 20 кластеров, силуэт **0.507253**. Модель, расстояние, метки и выполненные отчёты — `models/olist-stream/clustering/temporal/batch_000/`.
- Сохранённые истории — `data/olist-stream/histories/step_{000,001}/`. Побайтовая воспроизводимость обоих снимков и точное сохранение прежних историй на втором шаге проверены.
- Готовые Random/SVD-модели и оценки validation/test — `models/olist-stream/ranking/{random,svd}/step_{000,001}/`. Полные метрики и команды находятся в README. `data/`, `dataset/` и `models/` игнорируются Git.
- Повторные fit SVD в новом процессе с одним потоком BLAS/OpenMP дали побайтово одинаковые модели и манифесты на обоих шагах. Все top-10 и CSV оценок совпали, JSON оценок — с учётом нового пути модели. Проверены одинаковые входы Random/SVD и сохранность исходных историй и прежних артефактов.

| Снимок историй | Пользователей | Событий | Товаров | Random test Recall@10 | SVD test Recall@10 |
| --- | ---: | ---: | ---: | ---: | ---: |
| `step_000` | 2 000 | 200 000 | 14 314 | 0.000753571 | 0.077418373 |
| `step_001` | 2 250 | 225 000 | 15 599 | 0.000725926 | 0.074438687 |

## 2. Глобальный оставшийся план

**Согласованный долгосрочный объём — MVP + Docker + GitHub Actions.** MVP — локальный Python-конвейер с файловыми хранилищами, обновлением по батчам, рекомендациями и историей качества. Задания: `320_MLOps_task_1.pdf` и `320_MLOps_task_2.pdf`; специализированные MLOps-платформы исключены условиями задания.

Пользователь ранее явно отложил завершение общего конвейера до моделирования датасетов и ранжирования. SVD завершён; следующий предложенный этап — MiniBatchKMeans и новое расстояние. Порядок остальных блоков ниже **предложен в обсуждении**, их детальные интерфейсы ещё предстоит определить.

### 2.1. SVD и сравнение ранжировщиков — завершено

Обучаемый ранжировщик на счётчиках train-покупок подключён к общему сохранению и оценке. Сравнение с RandomRanker на двух снимках, проверки и воспроизводимость завершены. Результаты и интерфейс — [README](README.md#svd-на-счётчиках-покупок).

### 2.2. Второе пространство признаков и MiniBatchKMeans

Сохранённый отложенный план — [README: MiniBatchKMeans](README.md#minibatchkmeans-отложенный-план):

- 11 выбранных числовых признаков: цена, доставка, координаты покупателя и продавца, число фотографий, вес и размеры товара. Полный список — в README.
- Категория товара — `product_category_name`, полный one-hot. Служебные ключи и длины названия/описания товара в признаки модели не входят.
- `ColumnTransformer(remainder="drop")`: числа через `SimpleImputer` → `StandardScaler`, категория через `SimpleImputer` → `OneHotEncoder(drop=None, handle_unknown="ignore")`. One-hot отдельно не масштабируется.
- Предобработка обучается на первом батче; масштабы, категории и порядок признаков фиксируются и сохраняются вместе с моделью. На текущем первом батче ожидаются 31 признак после преобразования.
- Исходный план — MiniBatchKMeans с 20 кластерами, число принадлежит реализации. Сохранение и независимая оценка используют готовые интерфейсы кластеризации.

**Открытые решения:** расстояние, конкретные параметры, переобучение или `partial_fit`, пересчёт либо сохранение старых меток. Ранее предлагались seed 42, `n_init=10`, внутренний `batch_size=1024`, медианная импутация чисел и мода категории; это кандидаты, а не окончательная спецификация.

После появления другого расстояния предложен отдельный эксперимент генерации историй и сравнения Random/SVD. Температуру нужно калибровать в единицах нового расстояния. Внутри каждого эксперимента ранжировщики используют одинаковые истории и каталог.

### 2.3. Общий последовательный запуск

Связать готовые этапы в воспроизводимый сценарий:

- Первый шаг: создание эталона и EDA → модельный этап и его оценка → генерация историй → обучение ранжировщиков → независимая оценка.
- Следующие шаги: DEDA до добавления → обновление эталона и EDA → обновление моделей и оценка → добавление новых историй → обучение ранжировщиков → независимая оценка.

**Открыто:** интерфейс запуска, выбор очередного батча, передача артефактов и учёт завершённых шагов. Сейчас пути задаются явно; `init`/`update` работают только с эталоном и отчётами, `state.json` не читают и не продвигают. Расширять их до полного цикла ранее было признано преждевременным.

### 2.4. Пользовательский inference

Оформить самостоятельное получение рекомендаций из сохранённой модели: загрузка → `user_id` и K → ранжированный список товаров. Основа — существующий `predict(user_id, k)`. Название команды, её аргументы и формат вывода пока не зафиксированы.

### 2.5. Итоговая сводка summary

Собрать качество моделей по шагам, сравнение Random/SVD, динамику Recall/NDCG и ссылки на отчёты с идентичностью данных и настройками. JSON/CSV отдельных оценок уже есть. Формат общей сводки, графики и пользовательский интерфейс — открытые решения; чтение старых HTML для управления конвейером не требуется.

### 2.6. Полный эксперимент на потоке

Пройти все девять подготовленных батчей с сохранением моделей и результатов каждого шага. При текущих настройках генерации итог — **4 000 пользователей, 400 000 событий**, из них 280 000 train, 60 000 validation и 60 000 test. Проверить накопление, сохранность старых историй и воспроизводимость сквозного запуска.

### 2.7. Docker и GitHub Actions

- Docker: воспроизводимое окружение и запуск с файловыми входами и сохранением результатов.
- GitHub Actions: автоматические проверки и согласованный сценарий CI/CD.
- Конкретные workflow, получение данных, публикация артефактов и сценарий CD пока не определены. Перед реализацией обратиться к второму заданию.

**Предложенная оставшаяся последовательность:** MiniBatchKMeans и новое расстояние → общий запуск, inference и summary → полный поток → Docker → GitHub Actions.

## 3. Принятые решения

### Данные и эталон

- Единица исходных данных — позиция заказа, ключ `(order_id, order_item_id)`. Рабочая схема из 35 колонок, типы, время и ключи определены в `src/prak/schema.py`.
- Эталон — один физический CSV с уже поступившими батчами. `working_dataset.csv` содержит будущие строки и хвост, поэтому не заменяет эталон накопленного шага.
- `init` может явно перезаписать эталон. `update` строит DEDA до добавления и EDA после записи. Корректность проверяется fail-fast; пересечения ключей блокируют обновление, обнаруженный дрейф — информационный результат.
- EDA/DEDA — реально выполненные notebook и самодостаточные HTML. Старые отчёты предназначены человеку; новый шаг работает с текущими CSV. Ровно три графика на отчёт и метрики дрейфа описаны в README.
- В `init`/`update` принято простое поведение при ошибках: без автоматических повторов и отката уже сохранённого эталона. Снимки генерации и ранжирования отдельно публикуются атомарно в новые каталоги согласно своим готовым интерфейсам.

### Кластеризация и расстояние

- **Число кластеров принадлежит реализации алгоритма.** У временной модели есть `n_clusters`; CLI передаёт его только при явном `--temporal-n-clusters`. Генерация, оценка и общий координатор числа кластеров не требуют.
- Модели следуют sklearn-соглашениям: параметры конструктора, `fit`, `get_params`/`set_params`/`clone`, обученные атрибуты с `_`. Общий координатор делегирует способ обучения и обновления выбранной стратегии.
- Полное переобучение и пересчёт всех меток на накопленном эталоне — политика временного бейзлайна. Для других реализаций политика определяется отдельно; для бейзлайнов пользователь разрешил простой вариант.
- Расстояние имеет `fit` и `pairwise(X, Y=None)`. `TimestampDistance.fit` — no-op; `pairwise` вычисляет абсолютную разницу времени покупки в секундах.
- Оценке кластеризации достаточно готовых меток и расстояния. Обучения в ней нет. На выборку до 1000 строк выделяются 500 новым и 500 прежним с добором; силуэт и t-SNE используют одну выборку и одну исходную матрицу расстояний. Разные модели могут оцениваться со своими расстояниями — принятое учебное упрощение.

### Генерация историй

- **Ранжировщик связан с кластеризацией только через моделируемые данные.** Генератор использует пространство признаков и готовое расстояние; метки кластеров в вероятности не входят. Общее число пользователей независимо от числа кластеров.
- Каждый вызов семплирует якоря и покупки **только из нового батча**, создавая новых пользователей. Прежние пользователи, события, split и якоря сохраняются точно; изменение расстояния влияет только на новые истории.
- Якорь выбирается равномерно по строкам с возвращением. Вероятность позиции: `p_j ∝ exp(-d_j / temperature)`. Покупки семплируются с возвращением; расстояния считаются по одному якорю или небольшим блоком.
- Товар определяется через `product_id`. Повторные покупки допустимы, каждое событие имеет собственный ID и ссылку на исходную позицию. Синтетический `user_id` отличается от `customer_unique_id` Olist.
- Время разбиения — **синтетический порядок событий `event_index`**, а не сортировка по исходной дате Olist. Последнее принятое разбиение — train/validation/test по этому порядку.
- Каталог включает уникальные товары всех поступивших исходных батчей, включая не выбранные в истории. Якоря, источники и параметры находятся в `generator/`; они не являются входом ранжировщика.

Рабочие параметры пользователь поручил выбрать агенту; они уже откалиброваны и параметризованы:

| Параметр | Значение |
| --- | ---: |
| Новых пользователей на первом / следующем батче | 2 000 / 250 |
| Покупок на пользователя | 100 |
| Train / validation / test | 70 / 15 / 15 |
| Температура для TimestampDistance | 86 400 секунд |
| Seed | 42 |
| K оценки | 10 |

Протокол RNG и точные файловые схемы — в README. Эти числа использованы в завершённом сравнении Random/SVD. Для другого расстояния температура выбирается заново.

### Ранжирование и оценка

- Вход `fit(X, y=None)` — `RankingData(interactions, catalog)`: ровно `user_id`, `product_id` во взаимодействиях и отдельный полный каталог. Повторы взаимодействий сохраняются.
- Выдача — **`predict(user_id, k)`**, K вторым аргументом; массив уникальных товаров по порядку ранжирования. Ранее купленные товары разрешены. Неизвестный пользователь и K вне `1 … размер каталога` вызывают ошибку.
- RandomRanker строит воспроизводимую случайную перестановку каталога по seed и стабильному SHA-256 пользователя. Выдача независима от порядка вызовов и процесса, а при увеличении K сохраняет префикс.
- SVD использует разреженную матрицу **счётчиков train-покупок**, оценка товара — скалярное произведение пользовательского и товарного векторов. На каждом шаге — новый fit на накопленном train. Полный каталог включён в матрицу; товары без train-покупок имеют нулевые столбцы.
- SVD: пользователи и каталог сортируются по ID до fit; точно равные score разрешаются по `product_id`. Векторы товаров без train-покупок явно обнуляются. Размерность проверяется без автоматического уменьшения: каталог минимум из двух товаров, `1 <= n_components <= min(n_users, n_items)`, `n_iter >= 1`. Подробный контракт и обученные атрибуты — в README.
- Recall@K и NDCG@K используют бинарную релевантность по уникальным отложенным товарам и макроусреднение по пользователям. Не встречавшиеся в train товары сохраняются в ground truth.
- Ранжировщики сравниваются на одном сохранённом датасете, одинаковом каталоге и K. Validation — выбор настроек, test — итоговое измерение с зафиксированными настройками.
- Оценка вызывает только `predict`; обучения и чтения якорей нет. Файловая оценка проверяет тот же каталог и полный набор пользователей, что при обучении; в памяти допустимо подмножество известных пользователей.

## 4. Завершённый SVD и прежняя калибровка

**Текущая реализация:** `src/prak/ranking/svd.py`, экспорт `prak.ranking.SVDRanker`, CLI `rank --model svd --svd-n-components 32 --svd-n-iter 7`. Общие `train_ranker` и `report_ranking` переиспользованы без изменения форматов артефактов. Тесты модели — `tests/ranking/test_svd.py`; файловые и CLI-проверки расширены на оба ранжировщика.

Постоянный эксперимент использует `TruncatedSVD(n_components=32, n_iter=7, random_state=42)`, CSR со счётчиками `float32`, новый fit на накопленном train каждого шага, весь доступный каталог. BLAS/OpenMP ограничены одним потоком. Validation на шагах 000/001 совпал с прежней калибровкой до опубликованной точности: Recall@10 **0.073601 / 0.072532**, NDCG@10 **0.125939 / 0.123648**. Настройки зафиксированы после validation и до test. Реальные команды, метрики обеих моделей и артефакты — [README](README.md#измеренное-сравнение-randomsvd-на-olist--2026-09-22).

**Исторический расчёт в памяти на девяти батчах:** прежний исследовательский скрипт и обученные модели не сохранялись. Он использовал те же настройки и оценивал только validation. Его ориентир для конца потока (ещё не результат текущего SVDRanker):

| Шаг | Пользователей | Товаров | SVD validation Recall@10 | SVD validation NDCG@10 |
| --- | ---: | ---: | ---: | ---: |
| 008 | 4 000 | 25 448 | 0.060833 | 0.103718 |

Размеры выбраны по покрытию: на первом шаге увеличение стартовых пользователей с 1000 до 2000 уменьшало долю validation-покупок товаров вне всего train с 4.27% до 0.72%. Медианная train-история содержит 62 разных товара из 70 событий; около 21.84% уникальных validation-товаров пользователя уже встречались в его train.

В старой калибровке random был **аналитическим ожиданием** Recall@10 = `10 / размер каталога`; в завершённом сравнении использованы фактические результаты сохранённого RandomRanker. Диагностическая популярность давала около 0.0469 Recall@10 на первом шаге; отдельный popularity-ranker не входит в согласованный объём.

## 5. Ближайшие действия: MiniBatchKMeans и расстояние

1. **Проверить рабочее дерево и прочитать точки подключения.** `src/prak/clustering/models/temporal.py`, `distances/`, `training.py`, `evaluation.py`, `report.py`, `src/prak/generation/histories.py`, `src/prak/cli.py`. План признаков — раздел 2.2 и README.
2. **Определить открытые решения до реализации.** Расстояние в новом пространстве, параметры MiniBatchKMeans, полное переобучение либо `partial_fit`, судьба старых меток. Seed 42, `n_init=10`, `batch_size=1024`, медиана/мода — пока предложенные кандидаты. Предобработка первого батча должна сохраняться с фиксированными масштабами и категориями.
3. **Подключить модель и расстояние к существующим этапам.** Согласованная предобработка должна использоваться кластеризацией, независимой оценкой и генератором. Критерий завершения реализации — сохранение/загрузка артефактов, работа CLI, тесты на искусственных данных и воспроизводимая независимая оценка.
4. **Провести отдельный эксперимент новых историй.** Температуру откалибровать в единицах выбранного расстояния. Для Random/SVD использовать один новый снимок и каталог; результаты временного эксперимента сохранять в прежних каталогах. Настройки выбирать по validation, test оценивать после фиксации.
5. **Записать фактические результаты и следующие шаги.** Обновить README и handoff после проверок. Детальные критерии этого этапа уточняются вместе с решениями пункта 2; интерфейсы общего запуска, inference и summary остаются следующими задачами.

## 6. Материалы по следующим веткам работы

- **Кластеризация и расстояния:** `src/prak/clustering/models/temporal.py`, `distances/temporal_distances.py`, `training.py`, `evaluation.py`, `report.py`; интерфейсы и квоты — [README](README.md#временной-бейзлайн-кластеризация-и-оценка).
- **Накопление историй:** `src/prak/generation/histories.py`, `storage.py`; форматы и происхождение событий — [README](README.md#модельные-пользовательские-истории).
- **Общий запуск и отчёты:** `src/prak/update.py`, `auto_eda/{eda,deda,notebook}.py`; текущая семантика ошибок — [README](README.md#deda-и-обновление-эталона).
- **Предметные поля и графики:** [feature_selection.md](feature_selection.md), [visualizations/olist-eda/data_context.md](visualizations/olist-eda/data_context.md). В контексте есть старое исследование; для новых отчётов действуют актуальные размеры и правила README.
- **Инфраструктурный этап:** задания `320_MLOps_task_1.pdf`, `320_MLOps_task_2.pdf`, окружение `pyproject.toml` и `uv.lock`. Установка — `uv sync --frozen`.

## 7. Suggested skills / рекомендуемые навыки

- **`implement-spec`** — при реализации следующего этапа после определения его открытых решений.
- **`codebase-design`** — если потребуется спроектировать интерфейсы следующего модельного этапа или общего запуска.
- **`plot-it`** — при создании или визуальной проверке графиков отчётов и будущей сводки.
- **`writing-for-agents`** — при обновлении handoff для следующей сессии.
